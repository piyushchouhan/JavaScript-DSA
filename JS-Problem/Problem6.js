// Difference between call(), bind(), and apply() in JavaScript

/**
 * call() invokes a function with a provided this value and arguments individually.
 * bind() does not execute the function immediately; it returns a new function with this bound.
 * apply() invokes a function with a provided this value and arguments as an array.

 */